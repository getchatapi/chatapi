# Generate with: openssl rand -base64 32
JWT_SECRET=

# Comma-separated allowed origins for browser clients. Use * for local dev.
ALLOWED_ORIGINS=*

# Called when a message arrives for an offline user. Leave blank to disable.
# WEBHOOK_URL=https://yourapp.com/hooks/chatapi
# WEBHOOK_SECRET=

